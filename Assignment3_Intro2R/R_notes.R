# each line has to be run one by one using cmd+enter
# variable assignment 
a <- 2 
a =2
a
cat(a)
#no such thing as scalar object in R; everything is a vector 

#R script is like a bash script and will have the extention of .R or .r 
#R markdown is a mix or R code and txt 

